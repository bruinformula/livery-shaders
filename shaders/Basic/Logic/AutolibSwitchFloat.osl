shader AutolibSwitchFloat [[
    string category = "autolib"
]] (
    int Mask = 0 [[ string widget = "boolean" ]],
    float IfTrue = 0,
    float IfFalse = 0,
    output float Output = 0
) {
    Output = Mask ? IfTrue : IfFalse;
}
