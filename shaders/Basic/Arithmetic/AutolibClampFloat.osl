shader AutolibClampFloat [[
    string category = "autolib"
]] (
    float Input = 0.5,
    float Min = 0.0,
    float Max = 1.0,

    output float Output = 0
) {
    Output = clamp(Input, Min, Max);
}