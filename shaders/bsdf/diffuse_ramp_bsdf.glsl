#include "lib/mx_closure_type.glsl"

void diffuse_ramp_bsdf(
    ClosureData closureData,
    vec3 colorA,
    vec3 colorB,
    vec3 colorC,
    vec3 colorD,
    vec3 colorE,
    vec3 colorF,
    vec3 colorG,
    vec3 colorH,
    vec3 N, 
    inout BSDF Cout
) {
    return;
}